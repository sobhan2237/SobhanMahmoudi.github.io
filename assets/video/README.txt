Put your intro clip here as:

    intro.mp4

Recommended: H.264 MP4, under ~15MB, 16:9. The popup opens 5 seconds
after page load and looks for this exact filename automatically — no
code changes needed. If the file is missing, the popup shows a small
fallback note instead of a broken player, so the rest of the site
never breaks because of a missing video.
